{
  "name": "pruebas-codigo",
  "version": "1.0.0",
  "description": "mejorando la prueba de gentera",
  "scripts": {
    "one:browse": "npx parcel src/uno.index.html --port 3000 --lazy --open",
    "two:browse": "npx parcel src/dos.index.html --port 3001 --lazy --open",
    "one:build": "npx parcel build src/uno.index.html",
    "one:dev": "npx parcel src/uno.index.html --port 5000",
    "two:dev": "npx parcel src/dos.index.html --port 5001",
    "test": "npx jest --no-watchman --detectOpenHandles"
  },
  "author": "jguillendev@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@jest/globals": "^29.3.1",
    "@types/mocha": "^10.0.1",
    "jest": "^29.3.1",
    "parcel": "^2.8.3",
    "puppeteer": "^19.5.2",
    "puppeteer-core": "^19.5.2",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.4"
  }
}
