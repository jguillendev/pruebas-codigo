{
  "name": "pruebas-codigo",
  "version": "1.0.0",
  "description": "mejorando la prueba de gentera",
  "scripts": {
    "one:dev": "npx parcel src/uno.index.html --port 1234 --lazy --open",
    "two:dev": "npx parcel src/two.index.html --port 1235 --lazy --open",
    "one:build": "npx parcel build src/uno.index.html",
    "one:test": "npx parcel src/one.index.html --port 1234  --https",
    "two:test": "npx parcel src/two.index.html --port 1235  --https",
    "test": "jest"
  },
  "author": "jguillendev@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@jest/globals": "^29.3.1",
    "@types/mocha": "^10.0.1",
    "jest": "^29.3.1",
    "parcel": "^2.8.3",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.4"
  }
}
